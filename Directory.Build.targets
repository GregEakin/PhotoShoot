<Project>
  <!-- This file intentionally stops MSBuild from traversing parent directories
	   for Directory.Build.targets files, preventing stray global targets on the
	   build machine from interfering with this project. -->
</Project>
