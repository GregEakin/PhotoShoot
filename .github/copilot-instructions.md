# Copilot Instructions

## Project Guidelines
- For PhotoShoot, the FTP server already places files into a local folder; the app should monitor the local folder only and should not implement FTP access or syncing logic.